import { ErrorSnackbarDirective } from './error-snackbar.directive';

describe('ErrorSnackbarDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorSnackbarDirective();
    expect(directive).toBeTruthy();
  });
});
