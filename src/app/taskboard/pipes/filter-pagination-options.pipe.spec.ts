import { FilterPaginationOptionsPipe } from './filter-pagination-options.pipe';

describe('FilterPaginationOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPaginationOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
